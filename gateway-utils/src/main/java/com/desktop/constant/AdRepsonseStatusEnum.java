package com.desktop.constant;

public enum AdRepsonseStatusEnum {
    //fixme 完善@徐贺

   /* public static AdRepsonseStatusEnum eval(String code) {
        return Arrays.stream(values())
                .filter(value -> StringUtils.equals(value.name(), code))
                .findFirst()
                .orElseThrow(() -> Exceptions.newBusinessException(String.format("错误的AD返回状态:%s", code)));
    }*/
}
